# AI 个人工作台 — Spec v1

> 本文件是八轮需求拷问后的收敛结果。凡未在本文件中的能力，v1 一律不做。

---

## 1. 目标与成功标准

| 项 | 结论 |
|---|---|
| 使用者 | 仅本人，单用户，无注册/权限/多租户 |
| 形态 | 本地 Web 应用 |
| 成功标准 | 连续多天我主动打开它 |
| 首要风险 | 内容不够好 → 我不再打开 |

**反目标（明确不做）**
- 不做通用产品、不做多人使用、不做账号体系
- 不做 AI 自动发现数据源
- 不做公众号 / 知乎 / 即刻等强反爬平台的抓取
- 不做显式点赞反馈
- 不做「AI 直接给调仓指令」这类需要我盲目信任的输出
- 不做插件式数据层抽象

---

## 2. 范围

### 2.1 v1 唯一有逻辑的模块：知识聚合

`两个入口` 在技术上**不是两套系统**，而是同一套抓取/打分管道 + 两组源配置 + 两个筛选视图。

- **入口 A：Agent 开发**
  - 关注内容：框架与工具动态 / 论文与方法 / 实战案例与踩坑 / 产品与行业动态
- **入口 B：大数据**
  - 关注内容：传统大数据技术栈（Hadoop/Spark/Flink/湖仓/Hive）+ 数据工程与实时计算、数据治理、调度编排

### 2.2 仅需外壳（无任何业务逻辑）

- 导航栏 + 两个模块的占位页：**基金监控**、**铜币拍卖**
- 占位页只显示「未启用」状态，不写任何抓取逻辑（其数据源可达性尚未验证）

### 2.3 源策略

- **只接入有 RSS / Atom / 公开接口的源**：技术博客、Newsletter、YouTube/B站频道、掘金类有 RSS 的社区、英文源（HN 等）
- **首批源仅用国内可直连的中文源**：国外源（HN、Google AI Blog、arXiv 等）在本机网络不可达，v1 一律不接（2026-09 实测结论，清单见 plan §15）
- 首批源写死在配置中，跑通后再考虑扩充
- **源给的时间戳也要当数据看，不能盲信**：有的 feed 把本地时间标成 GMT/UTC（InfoQ 中文实测如此，其时间戳整体超前 8 小时），这类源需要在配置里显式声明其时间所在时区，否则日报的显示时间与时效性打分会一起失真（2026-09-23 实测结论，见 plan §12 第 12 条）
- **不接入**公众号、知乎、即刻、小红书
- 每个源记录 `last_ok_at` / `fail_count`；连续失败在界面上标红提示

### 2.4 输出形态

- **今日日报**：首页展示当日 5-10 条精选，每条含「打分 + 一句为什么值得看」
- **全量历史**：所有抓到的条目入库，支持按关键词 / 时间 / 入口搜索
- **反馈**：仅隐式记录点击行为（点开了哪条），不做点赞/踩

---

## 3. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | |
| 服务 | FastAPI + Uvicorn | 本地起服务，浏览器访问 |
| 存储 | SQLite | 单文件，够用，无需额外运维 |
| 抓取 | httpx + feedparser | 只走 RSS/Atom，不做浏览器渲染 |
| 调度 | APScheduler | 随服务常驻；接受本机休眠导致漏跑，开机补拉 |
| 模型 | 国产付费 API（OpenAI 兼容协议） | base_url / api_key / model 全部走配置，可换厂商 |
| 前端 | Jinja2 服务端渲染 + 少量原生 JS | 不引入构建链，降低维护成本 |

---

## 4. 目录结构

```
deskhub/
├── app/
│   ├── main.py            # FastAPI 入口，注册路由、启动调度器
│   ├── config.py          # 配置读取（含源清单、模型配置）
│   ├── db.py              # SQLite 连接与建表
│   ├── models.py          # 数据表定义
│   ├── fetcher.py         # RSS 抓取、解析、入库
│   ├── scorer.py          # LLM 打分与理由生成
│   ├── scheduler.py       # 定时任务定义
│   ├── routers/
│   │   ├── digest.py      # 首页日报
│   │   ├── search.py      # 历史搜索
│   │   ├── sources.py     # 源管理（查看状态、启停）
│   │   └── placeholders.py# 基金 / 铜币 占位页
│   └── templates/
├── data/
│   └── deskhub.db
├── config/
│   └── sources.yaml       # 源清单
├── spec.md
└── requirements.txt
```

---

## 5. 数据模型

```sql
-- 源
sources(
  id INTEGER PK,
  name TEXT,
  url TEXT UNIQUE,          -- feed 地址
  module TEXT,              -- 'agent' | 'bigdata'
  enabled INTEGER DEFAULT 1,
  last_ok_at DATETIME,
  fail_count INTEGER DEFAULT 0,
  created_at DATETIME
)

-- 条目
items(
  id INTEGER PK,
  source_id INTEGER FK,
  module TEXT,
  title TEXT,
  url TEXT,
  url_hash TEXT UNIQUE,     -- 去重键
  published_at DATETIME,
  summary TEXT,             -- 源自带摘要或正文前 N 字
  fetched_at DATETIME,
  clicked_at DATETIME       -- 隐式反馈：我点开过
)

-- 打分
item_scores(
  item_id INTEGER PK FK,
  score REAL,               -- 0-100
  reason TEXT,              -- 一句「为什么值得看」
  model TEXT,
  prompt_version TEXT,
  scored_at DATETIME
)

-- 抓取日志
fetch_runs(
  id INTEGER PK,
  source_id INTEGER FK,
  started_at DATETIME,
  finished_at DATETIME,
  status TEXT,              -- ok | http_error | parse_error | timeout
  http_status INTEGER,
  items_new INTEGER,
  error TEXT
)
```

**去重规则（v1）**：`url_hash` 唯一约束。标题相似度去重留到 v2。

---

## 6. 抓取管道

1. 读取 `enabled=1` 的源
2. 逐个抓取（串行 + 超时 10s，失败不阻塞其他源）
3. 解析条目 → 计算 `url_hash` → 已存在则跳过
4. 新条目入库（`items`，`module` 继承自源的配置）
5. 写 `fetch_runs` 日志；成功则更新源 `last_ok_at` 并清零 `fail_count`，失败则 `fail_count += 1`
6. 对**当日新增且未打分**的条目调用 LLM 打分
7. 生成当日日报视图

**失败处理**：单源失败只记录日志，不影响整体；源连续失败 ≥3 次在界面上标红。

**补拉**：服务启动时先执行一次抓取（覆盖本机休眠期间漏跑的情况）。

---

## 7. LLM 打分

- **输入**：条目标题 + 摘要（截断控制 token）+ `module` + 该入口的关注点描述
- **输出**：严格 JSON

```json
{ "score": 82, "reason": "讲了多智能体任务拆分的三种失败模式，和你正在做的调度相关" }
```

- **打分口径**：与入口关注点的相关度、信息密度、时效性；理由必须具体到「这条讲了什么、为什么和你有关」，禁止「内容优质」这类空话（理由是我以后校准排序的唯一依据）
- **成本**：每条一次调用，入池量 × 单条 token，预期每天几毛到一两元
- **失败兜底**：单条打分失败不影响其他条目；`item_scores` 缺失的条目在日报中排在末尾
- **排序**：按 score 降序，取前 5-10 条进日报；未打分的排在最后

---

## 8. 页面

| 页面 | 内容 |
|---|---|
| 首页 / 日报 | 今日精选 5-10 条（标题、来源、打分、理由、链接）；点击链接时记录 `clicked_at` |
| 入口视图 | Agent / 大数据两个筛选视图，展示该入口的今日列表 |
| 历史搜索 | 关键词 + 时间范围 + 入口筛选，结果分页 |
| 源管理 | 源列表、启停开关、最近成功时间、失败次数（标红） |
| 基金 / 铜币 | 占位页，仅显示「未启用」 |

---

## 9. 调度

- 服务启动时立即执行一次抓取（补拉）
- 之后按固定时间执行；具体时间见 §11 待定参数
- 调度随服务进程存活；本机休眠/关机期间不执行，醒来后由启动抓取兜底

---

## 10. 验收标准（v1 完成定义）

1. 配置里写上 3-5 个真实源，能抓到条目并入库
2. 新条目能被 LLM 打分，且理由具体、可反驳
3. 首页能看到当日 5-10 条精选，点击能跳转且被记录
4. 历史搜索能按关键词搜到过去抓的条目
5. 某个源挂掉时，页面能标红提示，且不影响其他源
6. **连续多天我主动打开它**

---

## 11. 待定参数（由你填写）

| 参数 | 说明 | 建议值 | 你的设定 |
|---|---|---|---|
| 每日出报时间 | 定时任务触发点 | 启动时跑一次 + 每天 08:00 | 启动时跑一次 + 每天 08:00|
| 每日入池量目标 | 入池太少则无从筛选 | ≥ 100 条/天 | 候选池 ≥ 30 条/天（打分后可供排序的条目数），不设抓取量硬下限|
| 历史保留策略 | 是否清理旧数据 | 全量保留 | 全量保留 + 提供手动 purge 脚本，不做自动清理|
| LLM 厂商与模型 | 国产付费 API | 任意 OpenAI 兼容接口 | deepseek-v4-flash|

---

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| 内容不够好 → 不再打开（首要风险） | **先做内容质量，最后做外壳**；开发顺序见 §13 |
| 源失效 | 记录 `last_ok_at` / `fail_count`，界面标红；单源失败不阻塞整体 |
| 本机常驻不可靠 | 启动时补拉；接受漏天 |
| 排序逻辑无人校准 | 隐式点击记录 + 理由文本；后续用点击分布调整 prompt |
| 已有替代方案够用 | 若跑通后仍不如直接刷 RSS，应果断停做，不要沉没成本 |

---

## 13. 开发顺序（重要）

里程碑清单里包含「外壳入口」，但**顺序必须反过来**：

1. **先蹚管道**：SQLite 建表 + 抓 3-5 个真实源 + 入库
2. **再上打分**：接 LLM，产出打分与理由，验证理由是否可反驳
3. **再做日报首页**：能看到 5-10 条精选，并能点击
4. **补历史搜索**
5. **最后做外壳**：两个入口视图、导航、基金/铜币占位页

顺序搞反会得到一个漂亮的空壳。
